# Database Layer

This directory contains SQLite database connection, migration runner, and schema definitions.

## Files (to be created in Phase 1):
- `sqlite.ts` - SQLite connection and helper functions
- `migrations.ts` - Migration runner
- `schema.sql` - SQL schema definitions
